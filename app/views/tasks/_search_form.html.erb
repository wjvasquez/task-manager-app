<%= search_form_for @q do |f| %>
  <%= f.label :status %>
  <%= f.select :status_eq, options_for_select(
    ['pending', 'completed'], params[:q] ? params[:q][:status_eq] : nil
    ), include_blank: true %>

  <%= f.submit "Filter" %>
  <%= link_to "Clear Filters", tasks_path %>
<% end %>
